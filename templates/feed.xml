<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ config.title }}</title>
  <link href="{{ config.base_url }}/feed.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ config.base_url }}"/>
  <updated>{{ last_updated | date(format="%+") }}</updated>
  <id>{{ config.base_url }}/feed.xml</id>
  <author>
    <name>{{ config.author }}</name>
  </author>
  {% for page in pages %}
  <entry>
    <title>{{ page.title }}</title>
    <published>{{ page.date | date(format="%+") }}</published>
    <updated>{% if page.updated %}{{ page.updated | date(format="%+") }}{% else %}{{ page.date | date(format="%+") }}{% endif %}</updated>
    <link href="{{ page.permalink }}"/>
    <id>{{ page.permalink }}</id>
    {% if page.description %}
    <summary>{{ page.description }}</summary>
    {% elif page.extra.summary %}
    <summary>{{ page.extra.summary }}</summary>
    {% endif %}
    <content type="html">{{ page.content | escape }}</content>
  </entry>
  {% endfor %}
</feed>
